[project]
name = "sleeper-super"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pandas>=3.0.5",
    "pydantic-settings>=2.15.0",
]
[project.scripts]
sleeper-super = "sleeper_super.app:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13"]
build-backend = "uv_build"
